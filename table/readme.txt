table插件  
[依赖]: jquery
[功能]：
    1、有基本样式、斑马纹样式、带边框样式、带状态(成功、失败)样式。
    2、支持固定表头、固定列、同时固定表头和列
    3、支持多级表头
    4、支持合并行/列
    4、支持单选、多选checkbox
    5、支持列排序
    6、支持列筛选
    7、当行的内容过多时，并且不想显示滚动条时，支持展开行功能
[events]
    
